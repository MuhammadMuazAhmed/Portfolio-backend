# Example environment variables for Vercel deployment
# Copy these to Vercel project settings and fill with your actual values

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
SMTP_SECURE=false

# Email Configuration
CONTACT_EMAIL=your-email@gmail.com
MAIL_FROM=your-email@gmail.com

# CORS Configuration
ALLOWED_ORIGINS=https://your-portfolio-domain.com,https://www.your-portfolio-domain.com

# Environment
NODE_ENV=production
